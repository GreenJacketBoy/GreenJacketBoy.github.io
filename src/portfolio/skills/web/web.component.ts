import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'sk-web',
  imports: [RouterModule],
  templateUrl: './web.component.html',
})

export class SkillWebComponent {
}
