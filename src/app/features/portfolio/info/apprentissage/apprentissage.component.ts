import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'if-apprentissage',
  imports: [RouterLink],
  templateUrl: './apprentissage.component.html',
})

export class InfoApprentissage {
}
